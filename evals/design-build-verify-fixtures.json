{
  "schemaVersion": "2.0",
  "cases": [
    {
      "id": "complete-stateful-change",
      "acceptedOutcomes": [
        "retry does not duplicate the notification",
        "recovery preserves the last authoritative state"
      ],
      "claimedOutcomes": [
        "retry does not duplicate the notification",
        "recovery preserves the last authoritative state"
      ],
      "architectureVerdict": "pass",
      "expectedBuildVerdict": "pass",
      "expectedOverallVerdict": "pass"
    },
    {
      "id": "narrow-happy-path-proof",
      "acceptedOutcomes": [
        "retry does not duplicate the notification",
        "recovery preserves the last authoritative state"
      ],
      "claimedOutcomes": [
        "retry does not duplicate the notification"
      ],
      "architectureVerdict": "pass",
      "expectedBuildVerdict": "fail",
      "expectedOverallVerdict": "fail"
    },
    {
      "id": "architecture-failure-stays-separate",
      "acceptedOutcomes": [
        "recovery preserves the last authoritative state"
      ],
      "claimedOutcomes": [
        "recovery preserves the last authoritative state"
      ],
      "architectureVerdict": "fail",
      "expectedBuildVerdict": "pass",
      "expectedOverallVerdict": "fail"
    }
  ]
}

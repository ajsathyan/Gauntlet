interface:
  display_name: "Implement PRD"
  short_description: "Execute an accepted product document"
  default_prompt: "Use $implement-prd to load the accepted product artifact once, compile bounded Tickets, and execute it through its authorized release stages."

# Product Decision Example

- Recommendation and practical effect: Show the existing stable machine label in the private dashboard so operators can distinguish provisioned machines without changing provisioning.
- Alternative and practical effect: A user-assigned nickname adds flexibility but also creates editing and persistence work, so keep it proposed unless that control is needed.
- Cannot verify: Whether the existing label is meaningful to operators; inspect representative rows before editing the PRD.
